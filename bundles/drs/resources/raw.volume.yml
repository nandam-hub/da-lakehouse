# Commented out until external location is created
# resources:
#   volumes:
#     raw_volume:
#       name: ${resources.schemas.raw_schema.name}
#       catalog_name: main
#       schema_name: ${resources.schemas.raw_schema.name}
#       storage_location: s3://amic-lh-sandbox-raw-catalog/goldengate/webdev/drs/
#       volume_type: EXTERNAL
#       comment: "External Volume where we land raw data from Golden Gate DRS."
#       grants:
#         - principal: ${var.service_principal_object_owner}
#           privileges:
#             - READ_VOLUME
#             - WRITE_VOLUME